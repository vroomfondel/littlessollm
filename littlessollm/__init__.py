"""littlessollm — a little SSO (+ API-JWT auth) for LiteLLM.

Minimal, MIT-only, config-driven OIDC UI-SSO and API-JWT->virtual-key auth
add-on for LiteLLM. No `litellm_enterprise` import, no enterprise license
gate bypassed — see README.md for the full design rationale.
"""

__version__ = "0.0.1"
